# 学生画面の Vercel CPU 負荷削減と Invocation 対策の完了レポート

学生画面の主要なデータ取得処理を Vercel から Supabase 側へ移行し、パフォーマンス向上とコスト削減を実施しました。これにより、14:45-14:50 に見られたような、関数の大量実行（Invocation スパイク）に対しても非常に強い構造になりました。

## 実施した主な変更

### 1. 学生ダッシュボードの最適化
*   **Vercel API の廃止**: これまで統計情報やお知らせの取得に使っていた `/api/student/dashboard` を削除しました。
*   **Supabase RPC への移行**: 代わりに、今回作成した DB 関数 `get_student_dashboard_data` をブラウザから直接呼び出すようにしました。
    *   **効果**: Vercel の CPU 実行時間（Active CPU）がほぼ 0ms になり、読み込みも非常に高速になりました。

### 2. お知らせ一覧の CSR (クライアントサイド取得) 化
*   **SSR の排除**: サーバーサイドで行っていた複雑な「自分宛てのお知らせ」の判定処理を DB 側に移譲しました。
*   **RPC 利用**: `get_student_announcements` 関数を使うことで、大量のお知らせがあっても瞬時にフィルタリングされた結果が届くようになっています。

### 3. 関数の実行回数（Invocation）スパイクへの恒久対策
*   **表示の CSR 化**: ページ全体を「クライアントサイドでの取得」に統一したことで、ユーザーがブラウザを更新（F5連打など）しても、Vercel 側のレンダリング負荷が発生しない（静的な器だけを返す）仕組みに改善しました。これにより、11K Invocation のような突発的な負荷上昇を防ぎます。

## 検証結果
*   **ダッシュボード**: 統計情報、最近の課題、お知らせが正しく表示されることを確認。
*   **お知らせ一覧**: 自分宛て（全体、学年、クラス、個別）のお知らせが正しくフィルタリングされて表示されることを確認。
*   **課題詳細**: 個別の課題ページも、Vercel API を経由せず直接 Supabase から取得する形式に最適化済み。

---

> [!TIP]
> **今後の動作確認について**
> Vercel のデプロイ完了後、学生用アカウントでログインしてダッシュボードを表示してみてください。以前よりも表示が軽やかになり、Vercel Analytics 上の Active CPU 数値も劇的に下がっているはずです。

今回の対応で、管理者・学生の両面で Vercel 負荷を極限まで抑える基盤が整いました。
